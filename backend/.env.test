# Node environment
NODE_ENV=test

# MySQL Database
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=@Pril252018
DB_NAME=notes_app

# JWT secret for authentication
JWT_SECRET=testsecret

# Email configuration (for OTP/password reset tests)
EMAIL_USER=test@example.com
EMAIL_PASS=supersecret

# Optional: Server port for testing
PORT=5001
